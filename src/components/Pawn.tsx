import { Piece } from "./Piece";

export const Pawn = () => {
    // Cuando se mueve la fecha buscar los valid moves
    // en vez de buscar los valid moves a todas las piezas
    // Si el movimiento no es valido, mostrar un error o algo asi.
    const getValidMoves = () => {
        return [];
    }

    return (
        <Piece>

        </Piece>
    );
};